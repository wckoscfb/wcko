# Cloudflare Pages SPA fallback is configured automatically via the new
# Workers+Assets pipeline (`not_found_handling: "single-page-application"`).
# An explicit `/*  /index.html  200` catch-all here would be rejected by
# Wrangler as an "infinite loop" (code 10021), so we leave it out.
#
# This file is normally OVERWRITTEN by scripts/build-team-pages.mjs at build
# time with the per-country rewrite rules. It exists in source mostly as a
# placeholder so Vite has something to copy if the post-build script is
# skipped during local development.
